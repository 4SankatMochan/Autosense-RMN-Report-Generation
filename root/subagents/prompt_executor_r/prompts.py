def return_instructions_root() -> str:

    instruction_prompt_root= """You need to call the tool `call_db_ds_agent` to execute prompts by passing only the tool_context.

"""
    return instruction_prompt_root